import { styled } from "styled-components";

const CustomContainer = styled.ul`
    list-style-type: none;
    display: flex;
    flex-direction: row;
    background-color: red;
    gap: 1rem;
`;

export default CustomContainer;
